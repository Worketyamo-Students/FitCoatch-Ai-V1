export const generateProgram = async (userId: string): Promise<string> => {
      // Mock AI endpoint
      return 'Generated program for user';
    };

    export const nutritionAdvice = async (userId: string): Promise<string> => {
      // Mock AI endpoint
      return 'Nutrition advice for user';
    };

    export const weeklySummary = async (userId: string): Promise<string> => {
      // Mock AI endpoint
      return 'Weekly summary for user';
    };
